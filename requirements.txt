# Default to CPU for local development
# For GPU deployment, use requirements-gpu.txt directly
-r requirements-cpu.txt

# Testing (development only)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
